spring:
    datasource:
        url: jdbc:h2:mem:coffeecatalogue_test;MODE=MySQL
    sql:
        init:
            data-locations:
                - classpath:data-test.sql
    jpa:
        hibernate:
            ddl-auto: create-drop
